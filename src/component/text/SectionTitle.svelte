<div class="section-title svelte-w1ndhn">
	<slot></slot>
</div>

<style>
	.section-title{
		display: block;
		position: relative;
		font-weight: bold;
		font-size: 1.3em;
		color: var(--main-strongColor);
		border-left: 0.3em solid var(--main-strongColor);
		padding-left: 0.3em;
	}
</style>